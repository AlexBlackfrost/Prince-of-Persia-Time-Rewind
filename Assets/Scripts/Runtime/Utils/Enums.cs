using System;

public enum Direction { Left, Right, Down, Up}

public enum SwordState { InHand, InBack, Sheathing, Unsheathing}

public enum Bool { False = 0, True = 1 } //Used for Animation Events because you cannot serialize a bool

